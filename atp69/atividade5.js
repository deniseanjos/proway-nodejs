// Crie um arquivo adicione uma lista de nomes. Imprima o valor dos elementos no terminal utilizando a função forEach.

const nomes = ["Jeca", "Tatu", "Irmão", "Jorel"];

nomes.push("Tody");

nomes.forEach(e => console.log(e));